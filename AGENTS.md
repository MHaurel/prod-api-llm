# Development Guidelines

- Do only what the user explicitly requests.
- Keep implementations simple and avoid unnecessary sophistication.
- Ask for missing information when it is needed to proceed correctly.
- Use `uv` to manage Python environments and dependencies.
